We want to test multiple ways
- if we have requirement, can we generate accurate code and test it against the csv file (input.csv)
- if we have code, then can we generate accurate requirements from it?
